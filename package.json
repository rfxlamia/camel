{
	"name": "camel-kanban",
	"private": true,
	"version": "0.1.0",
	"description": "Camel — Agentic Kanban",
	"workspaces": [
		"client",
		"server"
	],
	"scripts": {
		"dev": "npm run dev --workspace=server & wait-on tcp:localhost:3001 && npm run dev --workspace=client & wait",
		"dev:server": "npm run dev --workspace=server",
		"dev:client": "npm run dev --workspace=client",
		"db:up": "docker compose up -d db",
		"db:migrate": "npm run db:migrate --workspace=server",
		"db:seed": "npm run db:seed --workspace=server",
		"test": "npm run test --workspace=server && npm run test --workspace=client",
		"lint": "biome lint .",
		"lint:fix": "biome check --write .",
		"typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=client",
		"build": "npm run build --workspace=server && npm run build --workspace=client",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.0",
		"husky": "^9.1.7",
		"wait-on": "^9.0.10"
	},
	"optionalDependencies": {
		"@biomejs/cli-darwin-x64": "^2.5.0",
		"@biomejs/cli-linux-arm64": "^2.5.0",
		"@biomejs/cli-linux-x64": "^2.5.0",
		"@biomejs/cli-linux-x64-musl": "^2.5.0",
		"@biomejs/cli-win32-arm64": "^2.5.0",
		"@biomejs/cli-win32-x64": "^2.5.0"
	}
}
